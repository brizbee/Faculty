package by.karavay.command;

import javax.servlet.http.HttpServletRequest;

public class GetTutorCourses implements ActionCommand {
    @Override
    public String execute(HttpServletRequest req) {
        String page = null;
        //
        return page;
    }
}
