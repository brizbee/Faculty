package by.karavay.command;

import javax.servlet.http.HttpServletRequest;

public class UpdateFacultyForTeach implements ActionCommand {
    @Override
    public String execute(HttpServletRequest req) { // update teach
        String page = null;
        return page;
    }
}
