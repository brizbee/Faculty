package by.karavay.command;

import javax.servlet.http.HttpServletRequest;

public class GetStudentFaculty implements ActionCommand {
    @Override
    public String execute(HttpServletRequest req) {
        String page = null;
        return page;
    }
}
